mat_plot=t(mat)
image(mat_plot, col=gray(seq(0, 1, length=256)),
axes=FALSE, useRaster=TRUE,asp=1)
# Phase encoding gradient
delta_f_max = 2
T_grad = 1/(2*delta_f_max)
delta_f_step = delta_f_max/(n-1)
#delta_f = seq(-delta_f_max+delta_f_steps/2,delta_f_max-delta_f_steps/2,delta_f_steps)
#delta_f = seq(delta_f_max-delta_f_steps/2,-delta_f_max+delta_f_steps/2,-delta_f_steps)
delta_f = seq(-delta_f_max,delta_f_max-delta_f_step/2,delta_f_step)
n_samples = length(delta_f)
delta_f_FOV = (n-1)*delta_f
grad_phase = t(sapply(delta_f_FOV, function(f) seq(0,f, length.out = n)))
# Frequency encoding gradient
# Assuming delta_f_max is our maximum available "power"
f_max = delta_f_max*(n-1)
# Gradient in space
grad_freq = seq(0, f_max, length.out = n)
dt = 1 / (2*f_max)
t_max = 1/delta_f_max*1/2
#t_max = T_grad - T_grad/(2*n_samples)
sampled_time = seq(-t_max, t_max-dt/2, dt)
# Initialize array
kspace = array(0,dim=c(n_samples,n_samples))
# Phase encoding steps
for (f_idx in 1:n_samples){
## Vary y gradient
#for (f_idx_y in 1:n_samples) {
#Initialize vector
amps = array(0,dim=c(n^2,n_samples))
idx = 1
# Loop over all pixel
for (row_idx in 1:n) {
for (col_idx in 1:n){
# Get Amplitude from the image
amp = mat[row_idx, col_idx]
# Get phase at time point T_grad
# The frequencies from x and y gradients simply add up
phase = 2*(grad_phase[f_idx,row_idx]*T_grad + grad_freq[col_idx]*sampled_time)
if (row_idx == 2 & col_idx ==4){
phase = phase + 1.7*pi
}
# Amplitude at time point T_grad
amps[idx,] = amp*cos(pi*phase)
idx = idx+1
}
}
# Sum individual signals, put in k-space matrix
# y --> rows, x --> columns
kspace[f_idx,] = colSums(amps)
}
## 4. Plot k-space ##
#kspace_plot = t(apply(kspace, 2, rev))
kspace_plot = t(kspace)
df = expand.grid(x = 1:ncol(kspace_plot), y = 1:nrow(kspace_plot))
df$val = as.vector(kspace_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
## 5. Reconstruct image ##
# Perform Fourier transform
fft_result = fft(fftshift(kspace))
# Take only positive frequencies
img_rec = abs(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
#img_rec = Re(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
img_rec = Arg(fft_result[2: (ncol(fft_result)/2),2: (nrow(fft_result)/2)])
#img_rec = abs(fft_result)
# For correct orientation when plotting
img_rec_plot = t(apply(img_rec, 2, rev))
img_rec_plot = t(img_rec)
#Plot the reconstructed image
df = expand.grid(x = 1:ncol(img_rec_plot), y = 1:nrow(img_rec_plot))
df$val = as.vector(img_rec_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
img_rec
1.7*pi
1.7*pi/2
n = 5
mat = matrix(c(0, 0, 0, 0,0,
0, 0.6, 1, 0.3, 0,
0, 0.3, 0.6, 1, 0,
0, 1, 0.6, 0.3, 0,
0, 0, 0, 0, 0), nrow = n, byrow = TRUE)
n = 6
mat = matrix(c(0,   0,   0,   0,   0, 0,
0, 0.3,   1, 0.3, 0.6, 0,
0, 0.6,   1,   1,   1, 0,
0, 0.3, 0.6, 0.3, 0.3, 0,
0, 0.3, 0.6,   1, 0.3, 0,
0,   0,   0,   0,   0, 0), nrow = n, byrow = TRUE)
mat = apply(mat,2,rev)
#mat_plot = t(apply(mat, 2, rev)) # for correct orientation
mat_plot=t(mat)
image(mat_plot, col=gray(seq(0, 1, length=256)),
axes=FALSE, useRaster=TRUE,asp=1)
# Phase encoding gradient
delta_f_max = 2
T_grad = 1/(2*delta_f_max)
delta_f_step = delta_f_max/(n-1)
#delta_f = seq(-delta_f_max+delta_f_steps/2,delta_f_max-delta_f_steps/2,delta_f_steps)
#delta_f = seq(delta_f_max-delta_f_steps/2,-delta_f_max+delta_f_steps/2,-delta_f_steps)
delta_f = seq(-delta_f_max,delta_f_max-delta_f_step/2,delta_f_step)
n_samples = length(delta_f)
delta_f_FOV = (n-1)*delta_f
grad_phase = t(sapply(delta_f_FOV, function(f) seq(0,f, length.out = n)))
# Frequency encoding gradient
# Assuming delta_f_max is our maximum available "power"
f_max = delta_f_max*(n-1)
# Gradient in space
grad_freq = seq(0, f_max, length.out = n)
dt = 1 / (2*f_max)
t_max = 1/delta_f_max*1/2
#t_max = T_grad - T_grad/(2*n_samples)
sampled_time = seq(-t_max, t_max-dt/2, dt)
# Initialize array
kspace = array(0,dim=c(n_samples,n_samples))
# Phase encoding steps
for (f_idx in 1:n_samples){
## Vary y gradient
#for (f_idx_y in 1:n_samples) {
#Initialize vector
amps = array(0,dim=c(n^2,n_samples))
idx = 1
# Loop over all pixel
for (row_idx in 1:n) {
for (col_idx in 1:n){
# Get Amplitude from the image
amp = mat[row_idx, col_idx]
# Get phase at time point T_grad
# The frequencies from x and y gradients simply add up
phase = 2*(grad_phase[f_idx,row_idx]*T_grad + grad_freq[col_idx]*sampled_time)
if (row_idx == 2 & col_idx ==4){
phase = phase + 1.7*pi
}
if (row_idx == 4 & col_idx ==3){
phase = phase + 1.7*pi
}
# Amplitude at time point T_grad
amps[idx,] = amp*cos(pi*phase)
idx = idx+1
}
}
# Sum individual signals, put in k-space matrix
# y --> rows, x --> columns
kspace[f_idx,] = colSums(amps)
}
## 4. Plot k-space ##
#kspace_plot = t(apply(kspace, 2, rev))
kspace_plot = t(kspace)
df = expand.grid(x = 1:ncol(kspace_plot), y = 1:nrow(kspace_plot))
df$val = as.vector(kspace_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
## 5. Reconstruct image ##
# Perform Fourier transform
fft_result = fft(fftshift(kspace))
# Take only positive frequencies
img_rec = abs(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
#img_rec = Re(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
img_rec = Arg(fft_result[2: (ncol(fft_result)/2),2: (nrow(fft_result)/2)])
#img_rec = abs(fft_result)
# For correct orientation when plotting
img_rec_plot = t(apply(img_rec, 2, rev))
img_rec_plot = t(img_rec)
#Plot the reconstructed image
df = expand.grid(x = 1:ncol(img_rec_plot), y = 1:nrow(img_rec_plot))
df$val = as.vector(img_rec_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
img_rec
n = 5
mat = matrix(c(0, 0, 0, 0,0,
0, 0.6, 1, 0.3, 0,
0, 0.3, 0.6, 1, 0,
0, 1, 0.6, 0.3, 0,
0, 0, 0, 0, 0), nrow = n, byrow = TRUE)
n = 6
mat = matrix(c(0,   0,   0,   0,   0, 0,
0, 0.3,   1, 0.3, 0.6, 0,
0, 0.6,   1,   1,   1, 0,
0, 0.3, 0.6, 0.3, 0.3, 0,
0, 0.3, 0.6,   1, 0.3, 0,
0,   0,   0,   0,   0, 0), nrow = n, byrow = TRUE)
mat = apply(mat,2,rev)
#mat_plot = t(apply(mat, 2, rev)) # for correct orientation
mat_plot=t(mat)
image(mat_plot, col=gray(seq(0, 1, length=256)),
axes=FALSE, useRaster=TRUE,asp=1)
# Phase encoding gradient
delta_f_max = 2
T_grad = 1/(2*delta_f_max)
delta_f_step = delta_f_max/(n-1)
#delta_f = seq(-delta_f_max+delta_f_steps/2,delta_f_max-delta_f_steps/2,delta_f_steps)
#delta_f = seq(delta_f_max-delta_f_steps/2,-delta_f_max+delta_f_steps/2,-delta_f_steps)
delta_f = seq(-delta_f_max,delta_f_max-delta_f_step/2,delta_f_step)
n_samples = length(delta_f)
delta_f_FOV = (n-1)*delta_f
grad_phase = t(sapply(delta_f_FOV, function(f) seq(0,f, length.out = n)))
# Frequency encoding gradient
# Assuming delta_f_max is our maximum available "power"
f_max = delta_f_max*(n-1)
# Gradient in space
grad_freq = seq(0, f_max, length.out = n)
dt = 1 / (2*f_max)
t_max = 1/delta_f_max*1/2
#t_max = T_grad - T_grad/(2*n_samples)
sampled_time = seq(-t_max, t_max-dt/2, dt)
# Initialize array
kspace = array(0,dim=c(n_samples,n_samples))
# Phase encoding steps
for (f_idx in 1:n_samples){
## Vary y gradient
#for (f_idx_y in 1:n_samples) {
#Initialize vector
amps = array(0,dim=c(n^2,n_samples))
idx = 1
# Loop over all pixel
for (row_idx in 1:n) {
for (col_idx in 1:n){
# Get Amplitude from the image
amp = mat[row_idx, col_idx]
# Get phase at time point T_grad
# The frequencies from x and y gradients simply add up
phase = 2*(grad_phase[f_idx,row_idx]*T_grad + grad_freq[col_idx]*sampled_time)
if (row_idx == 2 & col_idx ==4){
phase = phase + 1.5*pi
}
if (row_idx == 4 & col_idx ==3){
phase = phase + 0.5*pi
}
# Amplitude at time point T_grad
amps[idx,] = amp*cos(pi*phase)
idx = idx+1
}
}
# Sum individual signals, put in k-space matrix
# y --> rows, x --> columns
kspace[f_idx,] = colSums(amps)
}
## 4. Plot k-space ##
#kspace_plot = t(apply(kspace, 2, rev))
kspace_plot = t(kspace)
df = expand.grid(x = 1:ncol(kspace_plot), y = 1:nrow(kspace_plot))
df$val = as.vector(kspace_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
## 5. Reconstruct image ##
# Perform Fourier transform
fft_result = fft(fftshift(kspace))
# Take only positive frequencies
img_rec = abs(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
#img_rec = Re(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
img_rec = Arg(fft_result[2: (ncol(fft_result)/2),2: (nrow(fft_result)/2)])
#img_rec = abs(fft_result)
# For correct orientation when plotting
img_rec_plot = t(apply(img_rec, 2, rev))
img_rec_plot = t(img_rec)
#Plot the reconstructed image
df = expand.grid(x = 1:ncol(img_rec_plot), y = 1:nrow(img_rec_plot))
df$val = as.vector(img_rec_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
img_rec
img_rec[1,3]/img_rec[3,2]
img_rec[1,3]-img_rec[3,2]
n = 5
mat = matrix(c(0, 0, 0, 0,0,
0, 0.6, 1, 0.3, 0,
0, 0.3, 0.6, 1, 0,
0, 1, 0.6, 0.3, 0,
0, 0, 0, 0, 0), nrow = n, byrow = TRUE)
n = 6
mat = matrix(c(0,   0,   0,   0,   0, 0,
0, 0.3,   1, 0.3, 0.6, 0,
0, 0.6,   1,   1,   1, 0,
0, 0.3, 0.6, 0.3, 0.3, 0,
0, 0.3, 0.6,   1, 0.3, 0,
0,   0,   0,   0,   0, 0), nrow = n, byrow = TRUE)
mat = apply(mat,2,rev)
#mat_plot = t(apply(mat, 2, rev)) # for correct orientation
mat_plot=t(mat)
image(mat_plot, col=gray(seq(0, 1, length=256)),
axes=FALSE, useRaster=TRUE,asp=1)
# Phase encoding gradient
delta_f_max = 2
T_grad = 1/(2*delta_f_max)
delta_f_step = delta_f_max/(n-1)
#delta_f = seq(-delta_f_max+delta_f_steps/2,delta_f_max-delta_f_steps/2,delta_f_steps)
#delta_f = seq(delta_f_max-delta_f_steps/2,-delta_f_max+delta_f_steps/2,-delta_f_steps)
delta_f = seq(-delta_f_max,delta_f_max-delta_f_step/2,delta_f_step)
n_samples = length(delta_f)
delta_f_FOV = (n-1)*delta_f
grad_phase = t(sapply(delta_f_FOV, function(f) seq(0,f, length.out = n)))
# Frequency encoding gradient
# Assuming delta_f_max is our maximum available "power"
f_max = delta_f_max*(n-1)
# Gradient in space
grad_freq = seq(0, f_max, length.out = n)
dt = 1 / (2*f_max)
t_max = 1/delta_f_max*1/2
#t_max = T_grad - T_grad/(2*n_samples)
sampled_time = seq(-t_max, t_max-dt/2, dt)
# Initialize array
kspace = array(0,dim=c(n_samples,n_samples))
# Phase encoding steps
for (f_idx in 1:n_samples){
## Vary y gradient
#for (f_idx_y in 1:n_samples) {
#Initialize vector
amps = array(0,dim=c(n^2,n_samples))
idx = 1
# Loop over all pixel
for (row_idx in 1:n) {
for (col_idx in 1:n){
# Get Amplitude from the image
amp = mat[row_idx, col_idx]
# Get phase at time point T_grad
# The frequencies from x and y gradients simply add up
phase = 2*(grad_phase[f_idx,row_idx]*T_grad + grad_freq[col_idx]*sampled_time)
if (row_idx == 2 & col_idx ==4){
phase = phase + 0.6*pi
}
if (row_idx == 4 & col_idx ==3){
phase = phase + 0.3*pi
}
# Amplitude at time point T_grad
amps[idx,] = amp*cos(pi*phase)
idx = idx+1
}
}
# Sum individual signals, put in k-space matrix
# y --> rows, x --> columns
kspace[f_idx,] = colSums(amps)
}
## 4. Plot k-space ##
#kspace_plot = t(apply(kspace, 2, rev))
kspace_plot = t(kspace)
df = expand.grid(x = 1:ncol(kspace_plot), y = 1:nrow(kspace_plot))
df$val = as.vector(kspace_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
## 5. Reconstruct image ##
# Perform Fourier transform
fft_result = fft(fftshift(kspace))
# Take only positive frequencies
img_rec = abs(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
#img_rec = Re(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
img_rec = Arg(fft_result[2: (ncol(fft_result)/2),2: (nrow(fft_result)/2)])
#img_rec = abs(fft_result)
# For correct orientation when plotting
img_rec_plot = t(apply(img_rec, 2, rev))
img_rec_plot = t(img_rec)
#Plot the reconstructed image
df = expand.grid(x = 1:ncol(img_rec_plot), y = 1:nrow(img_rec_plot))
df$val = as.vector(img_rec_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
img_rec
n = 5
mat = matrix(c(0, 0, 0, 0,0,
0, 0.6, 1, 0.3, 0,
0, 0.3, 0.6, 1, 0,
0, 1, 0.6, 0.3, 0,
0, 0, 0, 0, 0), nrow = n, byrow = TRUE)
n = 6
mat = matrix(c(0,   0,   0,   0,   0, 0,
0, 0.3,   1, 0.3, 0.6, 0,
0, 0.6,   1,   1,   1, 0,
0, 0.3, 0.6, 0.3, 0.3, 0,
0, 0.3, 0.6,   1, 0.3, 0,
0,   0,   0,   0,   0, 0), nrow = n, byrow = TRUE)
mat = apply(mat,2,rev)
#mat_plot = t(apply(mat, 2, rev)) # for correct orientation
mat_plot=t(mat)
image(mat_plot, col=gray(seq(0, 1, length=256)),
axes=FALSE, useRaster=TRUE,asp=1)
# Phase encoding gradient
delta_f_max = 2
T_grad = 1/(2*delta_f_max)
delta_f_step = delta_f_max/(n-1)
#delta_f = seq(-delta_f_max+delta_f_steps/2,delta_f_max-delta_f_steps/2,delta_f_steps)
#delta_f = seq(delta_f_max-delta_f_steps/2,-delta_f_max+delta_f_steps/2,-delta_f_steps)
delta_f = seq(-delta_f_max,delta_f_max-delta_f_step/2,delta_f_step)
n_samples = length(delta_f)
delta_f_FOV = (n-1)*delta_f
grad_phase = t(sapply(delta_f_FOV, function(f) seq(0,f, length.out = n)))
# Frequency encoding gradient
# Assuming delta_f_max is our maximum available "power"
f_max = delta_f_max*(n-1)
# Gradient in space
grad_freq = seq(0, f_max, length.out = n)
dt = 1 / (2*f_max)
t_max = 1/delta_f_max*1/2
#t_max = T_grad - T_grad/(2*n_samples)
sampled_time = seq(-t_max, t_max-dt/2, dt)
# Initialize array
kspace = array(0,dim=c(n_samples,n_samples))
# Phase encoding steps
for (f_idx in 1:n_samples){
## Vary y gradient
#for (f_idx_y in 1:n_samples) {
#Initialize vector
amps = array(0,dim=c(n^2,n_samples))
idx = 1
# Loop over all pixel
for (row_idx in 1:n) {
for (col_idx in 1:n){
# Get Amplitude from the image
amp = mat[row_idx, col_idx]
# Get phase at time point T_grad
# The frequencies from x and y gradients simply add up
phase = 2*(grad_phase[f_idx,row_idx]*T_grad + grad_freq[col_idx]*sampled_time)
if (row_idx == 2 & col_idx ==4){
phase = phase + 0.8*pi
}
if (row_idx == 4 & col_idx ==3){
phase = phase + 0.4*pi
}
# Amplitude at time point T_grad
amps[idx,] = amp*cos(pi*phase)
idx = idx+1
}
}
# Sum individual signals, put in k-space matrix
# y --> rows, x --> columns
kspace[f_idx,] = colSums(amps)
}
## 4. Plot k-space ##
#kspace_plot = t(apply(kspace, 2, rev))
kspace_plot = t(kspace)
df = expand.grid(x = 1:ncol(kspace_plot), y = 1:nrow(kspace_plot))
df$val = as.vector(kspace_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
## 5. Reconstruct image ##
# Perform Fourier transform
fft_result = fft(fftshift(kspace))
# Take only positive frequencies
img_rec = abs(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
#img_rec = Re(fft_result[1: (ncol(fft_result)/2+1),1: (nrow(fft_result)/2+1)])
img_rec = Arg(fft_result[2: (ncol(fft_result)/2),2: (nrow(fft_result)/2)])
#img_rec = abs(fft_result)
# For correct orientation when plotting
img_rec_plot = t(apply(img_rec, 2, rev))
img_rec_plot = t(img_rec)
#Plot the reconstructed image
df = expand.grid(x = 1:ncol(img_rec_plot), y = 1:nrow(img_rec_plot))
df$val = as.vector(img_rec_plot)
# Plot in ggplot
ggplot(df, aes(x = x, y = y, fill = val)) +
geom_raster(interpolate = FALSE) +
scale_fill_gradient(low = "black", high = "white") +
coord_fixed(expand = FALSE) +
theme_void() +
theme(legend.position = "none")
img_rec
img_rec[2,4]/pi
img_rec[4,2]/pi
img_rec[3,1]/pi
img_rec[1,3]/pi
img_rec[1,3]/(2*pi)
img_rec + min(img_rec)
img_rec[3,2]
img_rec[1,3]+abs(img_rec[3,2])
img_rec[1,3]+abs(img_rec[3,2])-pi
img_rec[3,2]-abs(img_rec[1,3])+pi
img_rec[3,2]
